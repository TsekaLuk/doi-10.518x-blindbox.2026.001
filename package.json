{
  "name": "vibe-hackathon",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:web": "bun run --cwd apps/web dev",
    "dev:server": "bun run --cwd apps/server dev",
    "build": "bun run --cwd apps/web build",
    "test": "bun test",
    "typecheck": "tsc -p packages/scene && tsc -p packages/shared && tsc -p packages/ai && tsc -p apps/server && tsc -p apps/web",
    "typecheck:tests": "tsc -p tsconfig.test.json",
    "check": "bun run typecheck && bun run typecheck:tests && bun run test && bun run build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.6.0"
  }
}
